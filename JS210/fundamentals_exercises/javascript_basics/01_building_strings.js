// The program raises a SyntaxError exception and will not execute. The extra spaces at the end of line 5 after the
// backslash prevent the newline character from being escaped. Additionally There are extra spaces at the beginning of the
// lines which are being interpreted as part of the string.