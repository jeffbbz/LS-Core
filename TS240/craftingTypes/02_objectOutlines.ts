let employee: { id: number, name: string, department: string } = {
  id: 23,
  name: "John Bongos",
  department: "HR",
};

// Here we use an object type annotation to define the shape of the `employee` object
// directly inline.