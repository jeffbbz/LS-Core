function greet(name: string, age: number): string {
  return `Hello, your name is ${name}, and you are ${age} years old.`;
}

// Here we define a function `greet` which takes two parameters `name` which is typed to `string`
// and `age` which is typed to `number`. The function is typed to return a `string` value.